configs.sunburst = {
    "type": "org.cishell.json.vis.metadata",
    
}

events.sunburst = function(ntwrk) {}

dataprep.sunburst = function(ntwrk) {
    ntwrk.filteredData.records.data.forEach(function(d, i) {
       
    })
}
